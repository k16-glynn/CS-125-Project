#include <stdio.h>
#include <string.h>
#include <ctype.h>

char words[3][50] = {"habitat", "icecaps", "rabbits"};

char* wordArray()
{
  int number = rand()%3;
  
  printf("%s\n", words[number]);
  
return words[number];
}

char userInput()
{
  char letter;
  printf("letter: ");
  scanf(" %c", &letter);
  
  
  while (!isalpha(letter)){
    printf("Invalid, try again: \n ");
    scanf(" %c", &letter);
  }
  
  letter = tolower(letter);
return letter;
}




int main()
{
  int i,c;
  char x[50] = "r a b b i t s";
  char z[50];
  //strcpy(x,wordArray());
  printf("%s\n\n", x); 
  char y ='b';
  
  for(c=0; c < strlen(x); c+=2)
  {
    z[c] = '_';
    z[c+1] = ' ';
  }
  
  z[c] = 0;
  
  printf("%s", z);
  printf("\n");
  
  for(i=0; i < strlen(x); i+=2)
  {
     if(x[i] == y)
     {
       printf("yes\n");
       z[i] = y;
     }
     
     else 
       printf("no\n");
  }
  
  printf("%s", z);
  printf("\n");
  
return 0;
}
