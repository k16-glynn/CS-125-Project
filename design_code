#include <stdio.h>
#include <string.h>

extern int wrongGuesses;

void hangmanFile()
{
  FILE*hangman = fopen("hangman.txt","r");

  int i;
  char line[100];

  switch(wrongGuesses)
  {
    case(0):
      for(i = 1; i<9; i++)
      {
        fgets(line,100,hangman);
        printf("%s",line);
      }
      break;
    
    case(1):
      for(i = 1; i<19; i++)
      {
        fgets(line,100,hangman);
        
        if(i>=11)
          printf("%s",line);
      }
      break;
      
    case(2):
      for(i = 1; i<29; i++)
      {
        fgets(line,100,hangman);
        
        if(i>=21)
          printf("%s",line);
      }
      break;
      
    case(3):
      for(i = 1; i<38; i++)
      {
        fgets(line,100,hangman);
        
        if(i>=30)
          printf("%s",line);
      }
      break;
      
    case(4):
      for(i = 1; i<48; i++)
      {
        fgets(line,100,hangman);
        
        if(i>=40)
          printf("%s",line);
      }
      break;
      
    case(5):
      for(i = 1; i<57; i++)
      {
        fgets(line,100,hangman);
        
        if(i>=49)
          printf("%s",line);
      }
      break;
      
    case(6):
      for(i = 1; i<67; i++)
      {
        fgets(line,100,hangman);
        
        if(i>=58)
          printf("%s",line);
      }
      break;
  }
  
  fclose(hangman);
}
